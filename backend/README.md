# Backend (Phase 2)

This folder is reserved for the FastAPI service:

- OpenCV frame extraction
- InsightFace / ArcFace embeddings
- Similarity search against registered missing-person embeddings
- Supabase PostgreSQL + Storage

Do not implement this until Phase 1 is approved. Face recognition must not use Gemini.
