import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register Person",
};

export default function RegisterPersonLayout({ children }: { children: React.ReactNode }) {
  return children;
}
